# LEVEL 1: Basics & Memory Understanding

1. Declare an integer array of size 10 and initialize it with zeros.

2. Read 5 integers into an array and print them in the same order.

3. Print all elements of an array using:

- a `for` loop
- a `while` loop

4. Print the memory address of each element in an array.

5. Find the size (in bytes) of:

- an integer array of size 10
- a character array of size 10

6. Show the difference between `arr` and `&arr` using `printf`.
