# Seeing memory
Here we are going to work on only seeing memory nothing else


```c
#include <stdio.h>

int main(){
        int x = 42;
        printf("x value: %d\n", x);
        printf("x address: %p\n", (void*)&x);
}
```
